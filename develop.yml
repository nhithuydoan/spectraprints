# requirements for deepseize's development environment
name: spectraprints
dependencies:
  - numpy
  - scipy
  - matplotlib
  - ipython
  - notebook
  # testing
  - pytest
  - pytest-cov
    # Linting & format
  - isort
  - mypy
  - pylint
  - spyder
  # pypi packages
  - pip
  - pip:
    - codespell
    - pytest-lazy-fixture
    - openseize
